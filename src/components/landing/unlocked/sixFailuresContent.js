import { buildLandingFailuresContent } from '@/utils/failures/buildLandingFailuresContent';

/** @deprecated Import buildLandingFailuresContent directly. Kept for landing grid compat. */
export const SIX_FAILURES = buildLandingFailuresContent();
