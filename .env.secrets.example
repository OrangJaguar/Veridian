# Copy to .env.secrets (gitignored) then run:
#   base44 secrets set --env-file .env.secrets
#
# NEVER prefix with VITE_ — that exposes secrets in the browser bundle.

GEMINI_API_KEY=your_google_ai_studio_api_key_here
