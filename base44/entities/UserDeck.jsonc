{
  "name": "UserDeck",
  "type": "object",
  "properties": {
    "userEmail": {
      "type": "string",
      "description": "Email of the owning user"
    },
    "title": {
      "type": "string"
    },
    "rawText": {
      "type": "string"
    },
    "parsedItems": {
      "type": "array",
      "items": {
        "type": "object"
      }
    },
    "lastEdited": {
      "type": "number"
    },
    "deckId": {
      "type": "string",
      "description": "client-side stable ID"
    }
  },
  "required": [
    "title",
    "rawText",
    "deckId"
  ],
  "rls": {
    "create": {
      "created_by": "{{user.email}}"
    },
    "read": {
      "created_by": "{{user.email}}"
    },
    "update": {
      "created_by": "{{user.email}}"
    },
    "delete": {
      "created_by": "{{user.email}}"
    }
  }
}