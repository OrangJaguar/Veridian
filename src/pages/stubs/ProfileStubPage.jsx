import StubPage from '@/components/stubs/StubPage';

export default function ProfileStubPage() {
  return (
    <StubPage
      title="Profile & Settings"
      phase="Phase 10"
      description="Account settings and your Learning Profile — knowledge map, trends, and study history — will be built in a later phase."
    />
  );
}
