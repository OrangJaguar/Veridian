import CollegeWorkspace from '@/components/tools/college/CollegeWorkspace';

export default function CollegeContent({ data, saveDocument }) {
  return <CollegeWorkspace data={data} saveDocument={saveDocument} />;
}
