/**
 * Deck → Journey migration (Phase 7+).
 * One UserDeck becomes one Journey with one Module and flashcardSet Activity.
 */
export async function migrateDeckToJourney(_deck) {
  throw new Error('deckToJourney migration not implemented yet');
}
